server {
    listen 80;
    server_name billing.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name billing.example.com;

    # SSL managed by aaPanel Let's Encrypt
    ssl_certificate     /www/server/panel/vhost/cert/billing.example.com/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/billing.example.com/privkey.pem;

    root /www/wwwroot/drp-billing/web;
    index index.html;

    client_max_body_size 20m;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    # Frontend SPA (history fallback for /admin/ and /client/)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API reverse proxy
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Branding uploads (logo/favicon) served by API from UPLOAD_DIR
    location /uploads/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SSE events (monitoring realtime)
    location /events/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding off;
    }

    access_log /www/wwwlogs/drp-billing.access.log;
    error_log  /www/wwwlogs/drp-billing.error.log;
}
